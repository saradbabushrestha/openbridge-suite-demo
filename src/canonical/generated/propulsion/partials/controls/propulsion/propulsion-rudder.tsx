import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../../../../runtime/openbridgeRegistry';

export function ObPropulsionPartialsControlsPropulsionPropulsionRudder(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="propulsion/partials/controls/propulsion/propulsion-rudder" {...props} />;
}

export default ObPropulsionPartialsControlsPropulsionPropulsionRudder;
