import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../../../../runtime/openbridgeRegistry';

export function ObPropulsionPartialsControlsPropulsionPropulsionTunnelThruster(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="propulsion/partials/controls/propulsion/propulsion-tunnel-thruster" {...props} />;
}

export default ObPropulsionPartialsControlsPropulsionPropulsionTunnelThruster;
