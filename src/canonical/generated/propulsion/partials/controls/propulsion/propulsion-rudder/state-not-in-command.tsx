import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../../../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../../../../../runtime/openbridgeRegistry';

export function ObPropulsionPartialsControlsPropulsionPropulsionRudderStateNotInCommand(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="propulsion/partials/controls/propulsion/propulsion-rudder/state-not-in-command" {...props} />;
}

export default ObPropulsionPartialsControlsPropulsionPropulsionRudderStateNotInCommand;
