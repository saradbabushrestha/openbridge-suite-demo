import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../../../../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../../../../../../runtime/openbridgeRegistry';

export function ObPropulsionPartialsControlsPropulsionPropulsionMainEngineStateOffFrameBackground(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="propulsion/partials/controls/propulsion/propulsion-main-engine/state-off/frame-background" {...props} />;
}

export default ObPropulsionPartialsControlsPropulsionPropulsionMainEngineStateOffFrameBackground;
