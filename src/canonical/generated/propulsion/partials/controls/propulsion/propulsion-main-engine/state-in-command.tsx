import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../../../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../../../../../runtime/openbridgeRegistry';

export function ObPropulsionPartialsControlsPropulsionPropulsionMainEngineStateInCommand(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="propulsion/partials/controls/propulsion/propulsion-main-engine/state-in-command" {...props} />;
}

export default ObPropulsionPartialsControlsPropulsionPropulsionMainEngineStateInCommand;
