import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsHeadingLineOffIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/heading-line-off-iec" {...props} />;
}

export default ObIconsHeadingLineOffIec;
