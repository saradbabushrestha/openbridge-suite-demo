import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsMotionTrueIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/motion-true-iec" {...props} />;
}

export default ObIconsMotionTrueIec;
