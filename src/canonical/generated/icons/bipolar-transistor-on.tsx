import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsBipolarTransistorOn(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/bipolar-transistor-on" {...props} />;
}

export default ObIconsBipolarTransistorOn;
