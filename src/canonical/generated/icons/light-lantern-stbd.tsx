import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsLightLanternStbd(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/light-lantern-stbd" {...props} />;
}

export default ObIconsLightLanternStbd;
