import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsPanelIlluminationIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/panel-illumination-iec" {...props} />;
}

export default ObIconsPanelIlluminationIec;
