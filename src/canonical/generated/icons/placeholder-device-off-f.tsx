import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsPlaceholderDeviceOffF(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/placeholder-device-off-f" {...props} />;
}

export default ObIconsPlaceholderDeviceOffF;
