import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsAisAtonPhysicalIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/ais-aton-physical-iec" {...props} />;
}

export default ObIconsAisAtonPhysicalIec;
