import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsMediaBackwards10s(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/media-backwards-10s" {...props} />;
}

export default ObIconsMediaBackwards10s;
