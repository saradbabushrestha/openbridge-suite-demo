import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsTargetAssociatedRadarCamera(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/target-associated-radar-camera" {...props} />;
}

export default ObIconsTargetAssociatedRadarCamera;
