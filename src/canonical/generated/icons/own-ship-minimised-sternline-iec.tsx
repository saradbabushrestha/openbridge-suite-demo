import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsOwnShipMinimisedSternlineIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/own-ship-minimised-sternline-iec" {...props} />;
}

export default ObIconsOwnShipMinimisedSternlineIec;
