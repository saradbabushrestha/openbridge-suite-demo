import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsIecChartDispSet(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/iec-chart-disp-set" {...props} />;
}

export default ObIconsIecChartDispSet;
