import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsElectricalClockOff(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/electrical-clock-off" {...props} />;
}

export default ObIconsElectricalClockOff;
