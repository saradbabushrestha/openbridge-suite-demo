import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsLightEmergencyColourOn(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/light-emergency-colour-on" {...props} />;
}

export default ObIconsLightEmergencyColourOn;
