import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsStabilisationIndicatorGroundIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/stabilisation-indicator-ground-iec" {...props} />;
}

export default ObIconsStabilisationIndicatorGroundIec;
