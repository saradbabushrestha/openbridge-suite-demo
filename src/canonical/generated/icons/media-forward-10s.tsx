import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsMediaForward10s(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/media-forward-10s" {...props} />;
}

export default ObIconsMediaForward10s;
