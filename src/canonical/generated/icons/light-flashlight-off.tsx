import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsLightFlashlightOff(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/light-flashlight-off" {...props} />;
}

export default ObIconsLightFlashlightOff;
