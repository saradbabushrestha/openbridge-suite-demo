import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsCapacitor02Off(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/capacitor-02-off" {...props} />;
}

export default ObIconsCapacitor02Off;
