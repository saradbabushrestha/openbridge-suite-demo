import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsLineOfPositionIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/line-of-position-iec" {...props} />;
}

export default ObIconsLineOfPositionIec;
