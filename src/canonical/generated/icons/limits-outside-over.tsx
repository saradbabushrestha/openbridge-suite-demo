import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsLimitsOutsideOver(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/limits-outside-over" {...props} />;
}

export default ObIconsLimitsOutsideOver;
