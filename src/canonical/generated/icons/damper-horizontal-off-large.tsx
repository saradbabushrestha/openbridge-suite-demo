import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsDamperHorizontalOffLarge(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/damper-horizontal-off-large" {...props} />;
}

export default ObIconsDamperHorizontalOffLarge;
