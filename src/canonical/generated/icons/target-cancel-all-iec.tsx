import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsTargetCancelAllIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/target-cancel-all-iec" {...props} />;
}

export default ObIconsTargetCancelAllIec;
