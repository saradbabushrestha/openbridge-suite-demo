import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIcons07ChartStyles(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/07-chart-styles" {...props} />;
}

export default ObIcons07ChartStyles;
