import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsPropulsionTunnelThruster(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/propulsion-tunnel-thruster" {...props} />;
}

export default ObIconsPropulsionTunnelThruster;
