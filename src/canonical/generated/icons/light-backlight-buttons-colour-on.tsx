import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsLightBacklightButtonsColourOn(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/light-backlight-buttons-colour-on" {...props} />;
}

export default ObIconsLightBacklightButtonsColourOn;
