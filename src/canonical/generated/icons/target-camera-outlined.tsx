import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsTargetCameraOutlined(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/target-camera-outlined" {...props} />;
}

export default ObIconsTargetCameraOutlined;
