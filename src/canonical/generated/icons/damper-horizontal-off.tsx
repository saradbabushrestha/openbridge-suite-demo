import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsDamperHorizontalOff(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/damper-horizontal-off" {...props} />;
}

export default ObIconsDamperHorizontalOff;
