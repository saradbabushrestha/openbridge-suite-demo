import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsDamperHorizontalOnLarge(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/damper-horizontal-on-large" {...props} />;
}

export default ObIconsDamperHorizontalOnLarge;
