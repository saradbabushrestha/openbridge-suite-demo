import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsTargetAssociatedAisCamera(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/target-associated-ais-camera" {...props} />;
}

export default ObIconsTargetAssociatedAisCamera;
