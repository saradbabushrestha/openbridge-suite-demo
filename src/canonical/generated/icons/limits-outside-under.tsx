import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsLimitsOutsideUnder(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/limits-outside-under" {...props} />;
}

export default ObIconsLimitsOutsideUnder;
