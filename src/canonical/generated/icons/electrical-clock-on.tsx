import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsElectricalClockOn(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/electrical-clock-on" {...props} />;
}

export default ObIconsElectricalClockOn;
