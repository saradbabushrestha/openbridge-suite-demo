import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsComMicrophoneFill(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/com-microphone-fill" {...props} />;
}

export default ObIconsComMicrophoneFill;
