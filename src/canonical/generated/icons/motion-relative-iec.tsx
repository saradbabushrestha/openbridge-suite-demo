import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsMotionRelativeIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/motion-relative-iec" {...props} />;
}

export default ObIconsMotionRelativeIec;
