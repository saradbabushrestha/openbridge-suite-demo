import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsTargetAssociatedCameraRadar(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/target-associated-camera-radar" {...props} />;
}

export default ObIconsTargetAssociatedCameraRadar;
