import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsCapacitor01On(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/capacitor-01-on" {...props} />;
}

export default ObIconsCapacitor01On;
