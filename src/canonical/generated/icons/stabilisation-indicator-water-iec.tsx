import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsStabilisationIndicatorWaterIec(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/stabilisation-indicator-water-iec" {...props} />;
}

export default ObIconsStabilisationIndicatorWaterIec;
