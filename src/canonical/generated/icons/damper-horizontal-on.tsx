import * as React from 'react';
import { OpenBridgeCanonicalAsset } from '../../../runtime/openbridgeCanonicalRegistry';
import type { OpenBridgeAssetProps } from '../../../runtime/openbridgeRegistry';

export function ObIconsDamperHorizontalOn(props: OpenBridgeAssetProps) {
  return <OpenBridgeCanonicalAsset asset="icons/damper-horizontal-on" {...props} />;
}

export default ObIconsDamperHorizontalOn;
