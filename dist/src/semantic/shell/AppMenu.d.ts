export type AppMenuProps = {
    expanded?: boolean;
    size?: 'small' | 'large';
    title?: string;
    footer?: React.ReactNode;
};
export declare function AppMenu({ expanded, size, title, footer }: AppMenuProps): any;
export default AppMenu;
